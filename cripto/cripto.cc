# include <iostream>
# include <fstream>
# include <string>

using namespace std; 

int main (int argc, char* argv[]){
    string arch_entrada = argv[1], arch_salida = argv[2], 
        modo_de_codificado = argv[3], contraseña = argv[4], modo = argv[5]; 
    ifstream archivo_de_entrada {arch_entrada}, archivo_de_salida{argv[2]};
return 0; 
}